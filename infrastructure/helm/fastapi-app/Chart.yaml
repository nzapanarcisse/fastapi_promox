apiVersion: v2
name: fastapi-app
description: Helm chart pour l'application FastAPI Full-Stack (Backend + Frontend + PostgreSQL)
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "16.2.1"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled

